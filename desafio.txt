Desafio
Campos:

Descrição: input text
Situação (pendente/finalizada): radio button

Requisitos:

OK Cadastrar uma tarefa
OK Exibir listagem de tarefas cadastras (descrição e situação)
OK Título do TODO list como one databind (variável no escopo para apresentar na view)
OK Excluir uma tarefa cadastrada
OK Editar uma tarefa cadastrada.
OK Botão que exibe somente as tarefas finalizadas
OK Tarefas pendentes devem possuir cor vermelha


Desafio extra:

Contador de total de tarefas por situação (pendente, finalizada e todas)
OK Armazenar em localstorage
OK Poder cadastrar mais situações
OK Poder carregar dinamicamente mais situações na view (regra: se houver mais de 2 situações o elemento se torna um combobox)

Outros:

Substituir show,hide -> if
Substituir $index por causa da paginação (colocar id)
Alterar/deletar situações
Arrumar quando array (em localstorage) for null


  <table>
    <tr>
      <th></th>
      <th> Descricao </th>
      <th> Situacao </th>
      <th> Opcao </th>
    </tr>

    <tr ng-repeat="item in itens | filter:{situacao: exibicao}" ng-class="{red: item.situacao=='pendente'}">
      <td><input type="checkbox" ng-model="item.selecionado"/></td>
      <td>
        <div ng-hide="$index==editar">{{item.descricao}}</div>
        <input type="text" size="5" ng-model="item.descricao" ng-show="$index==editar">
      </td>
      <td>
        <div ng-hide="$index==editar">{{item.situacao}}</div>
        <select ng-model="item.situacao" ng-show="$index==editar">
          <option ng-repeat="situacao in situacoes" title="{{situacao.descricao}}" value="{{situacao.descricao}}">{{situacao.descricao}}</option>
        </select>
      </td>
      <td>
        <button ng-click="editarItem($index)" ng-hide="$index==editar">Editar</button>
       <button ng-click="editarItem('item')" ng-show="$index==editar">Salvar</button>
      </td>
    </tr>
  </table>


<script src="http://code.angularjs.org/1.0.1/angular-1.0.1.min.js"></script>